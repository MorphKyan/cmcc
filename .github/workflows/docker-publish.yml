name: Build and Push Docker Images

on:
  push:
    branches: [ "master", "main" ]
  workflow_dispatch: # 允许手动点按钮触发

env:
  # 阿里云北京节点个人版实例地址
  REGISTRY: crpi-levx0ydbtxzjpzw9.cn-beijing.personal.cr.aliyuncs.com
  # 格式: 命名空间/镜像名
  IMAGE_NAME_BACKEND: morph-builds/cmcc-backend
  # ⚠️ 请确保你在阿里云控制台也创建了名为 cmcc-frontend 的仓库
  IMAGE_NAME_FRONTEND: morph-builds/cmcc-frontend

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 设置 Docker Buildx (支持多架构构建，且缓存更强)
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 构建并推送后端
      - name: Build and push Backend
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_BACKEND }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # 构建并推送前端
      - name: Build and push Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./Dockerfile.frontend
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_FRONTEND }}:latest
          # 传递构建参数
          build-args: |
             VITE_BACKEND_URL=/api
          cache-from: type=gha
          cache-to: type=gha,mode=max
